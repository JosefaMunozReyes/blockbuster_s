<p style="color: green"><%= notice %></p>

<div class="container">
  <div class="tab_head">
  <h1>Peliculas Rentadas</h1>
  </div>  
  <div id="movies">
    <div class="row">
      <% @movies.each do |movie| %>
      
        <%= render movie %>
      <% end %>
    </div>
  </div>
  <%= link_to "Agregar Pelicula", new_movie_path, class: "btn btn-primary" %>
</div>
